find|files|NATIVE|find . -name "*{ARG0}*"|SHELL|Get-ChildItem -Recurse -Filter *{ARG0}*|NATIVE|find . -name "*{ARG0}*"
show|processes|NATIVE|ps aux|SHELL|tasklist|NATIVE|ps aux
create|folder|NATIVE|mkdir "{ARG0}"|SHELL|mkdir "{ARG0}"|NATIVE|mkdir "{ARG0}"
list|files|NATIVE|ls -la|SHELL|dir|NATIVE|ls -la
delete|folder|NATIVE|rm -r "{ARG0}"|SHELL|Remove-Item -Recurse -Force "{ARG0}"|NATIVE|rm -r "{ARG0}"
move|file|NATIVE|mv "{ARG0}" "{ARG1}"|SHELL|Move-Item "{ARG0}" "{ARG1}"|NATIVE|mv "{ARG0}" "{ARG1}"
copy|file|NATIVE|cp "{ARG0}" "{ARG1}"|SHELL|Copy-Item "{ARG0}" "{ARG1}"|NATIVE|cp "{ARG0}" "{ARG1}"
compress|files|NATIVE|tar -czvf archive.tar.gz "{ARG0}"|SHELL|Compress-Archive -Path "{ARG0}" -DestinationPath archive.zip|NATIVE|tar -czvf archive.tar.gz "{ARG0}"
search|text|NATIVE|grep -r "{ARG0}" .|SHELL|Select-String -Path * -Pattern "{ARG0}" -AllMatches|NATIVE|grep -r "{ARG0}" .
show|disk|NATIVE|df -h|SHELL|Get-PSDrive -PSProvider FileSystem|NATIVE|df -h
